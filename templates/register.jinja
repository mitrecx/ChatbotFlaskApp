<!DOCTYPE html>
<html>

<head>
    <title>注册</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            max-width: 400px;
            margin: 50px auto;
            padding: 20px;
        }

        .form-group {
            margin-bottom: 15px;
        }

        input {
            width: 100%;
            padding: 8px;
            margin-top: 5px;
            border: 1px solid #ddd;
            border-radius: 4px;
        }

        button {
            background: #4CAF50;
            color: white;
            padding: 10px 20px;
            border: none;
            cursor: pointer;
            border-radius: 4px;
            width: 100%;
        }

        button:hover {
            background: #45a049;
        }

        a {
            color: #4CAF50;
            text-decoration: none;
        }

        .flash-message {
            color: red;
            margin-bottom: 15px;
        }
    </style>
</head>

<body>
    <h2>创建新账号</h2>

    <!-- 错误消息显示 -->
    {% with messages = get_flashed_messages() %}
    {% if messages %}
    <div class="flash-message">
        {% for message in messages %}
        {{ message }}
        {% endfor %}
    </div>
    {% endif %}
    {% endwith %}

    <!-- 注册表单 -->
    <form method="POST">
        <div class="form-group">
            <label>用户名:</label>
            <input type="text" name="username" required placeholder="请输入用户名" autocomplete="username">
        </div>

        <div class="form-group">
            <label>密码:</label>
            <input type="password" name="password" required placeholder="至少6位字符" autocomplete="new-password">
        </div>

        <button type="submit">立即注册</button>
    </form>

    <p style="margin-top: 20px; text-align: center;">
        已有账号？ <a href="{{ url_for('login') }}">去登录</a>
    </p>
</body>

</html>